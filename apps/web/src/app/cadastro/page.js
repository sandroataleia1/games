"use client";
import Link from "next/link"; import { useRouter, useSearchParams } from "next/navigation"; import { Suspense, useState } from "react"; import { api } from "../../lib/api"; import { safeInternalPath, withNext } from "../../lib/safe-redirect"; import styles from "../auth.module.css";
function CadastroForm(){const router=useRouter(),searchParams=useSearchParams(),next=safeInternalPath(searchParams.get("next"),"/jogos/quiz"),[error,setError]=useState(""),[busy,setBusy]=useState(false);async function submit(e){e.preventDefault();const f=new FormData(e.currentTarget);if(f.get("password")!==f.get("confirmation")){setError("As senhas não coincidem.");return}setBusy(true);setError("");try{await api("/auth/register",{method:"POST",body:JSON.stringify({name:f.get("name"),email:f.get("email"),password:f.get("password")})});router.replace(next)}catch(err){setError(err.message)}finally{setBusy(false)}}return <section className={styles.card}><span className={styles.brand}>QuizArena</span><h1>Criar conta</h1><p>Jogue, crie salas e organize seus próprios quizzes.</p><form className={styles.form} onSubmit={submit}><div className={styles.field}><label htmlFor="name">Nome</label><input id="name" name="name" required minLength={2}/></div><div className={styles.field}><label htmlFor="email">E-mail</label><input id="email" name="email" type="email" required/></div><div className={styles.field}><label htmlFor="password">Senha</label><input id="password" name="password" type="password" minLength={10} required/><small>Ao menos 10 caracteres, uma letra e um número.</small></div><div className={styles.field}><label htmlFor="confirmation">Confirmar senha</label><input id="confirmation" name="confirmation" type="password" required/></div><button className={styles.button} disabled={busy}>{busy?"Criando…":"Criar conta"}</button><p className={styles.error} role="alert">{error}</p></form><Link className={styles.link} href={withNext("/login",next)}>Já tenho uma conta</Link></section>}
export default function Cadastro(){return <main className={styles.shell}><Suspense fallback={null}><CadastroForm/></Suspense></main>}
